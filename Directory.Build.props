<Project>
  <PropertyGroup>
    <!-- Exclude ad-hoc build output folders created under project roots. -->
    <DefaultItemExcludes>$(DefaultItemExcludes);artifacts\**</DefaultItemExcludes>
  </PropertyGroup>
</Project>
