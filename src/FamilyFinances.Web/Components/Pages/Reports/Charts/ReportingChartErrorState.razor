<div class="card shadow-sm mb-4 border-danger" data-testid="reporting-chart-error">
    <div class="card-body py-4">
        <div class="d-flex align-items-start gap-3">
            <i class="bi bi-exclamation-triangle-fill text-danger fs-4"></i>
            <div>
                <h6 class="mb-1 text-danger">Unable to render annual charts</h6>
                <p class="mb-0 text-muted">@Message</p>
            </div>
        </div>
    </div>
</div>

@code {
    [Parameter] public string Message { get; set; } = "Chart data is unavailable right now.";
}
